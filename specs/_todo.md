* add a hand stop sign detected input and a rule that says that hand closes means to cancel current reaction or tools ?

* history management (cut history, compaction, etc...)

* store a current llm call (reaction) in the world with the trigger (with plan ?)
* update the state of the reaction (triggered, thinking, in progress) ?
* link reaction with commmand entries ?
* LLM call concurrency + LLM call cancellation

* add a command to the llm to be able to create plans and cancel plans

* list the world entries that a LLM reaction takes into account ?

* generic command entries in World with TTL linked to completion ?
* agent command to set an entry as taken into account by agent
* memory command for agent ?

* specific TTL for destruction after LLM trigger
* add a parameter next to LLM for destruction on LLM trigger and/or taken into account

* stream output
* use a speak command instead of output ?

* commands with subcommands + simultaneous tool calls
