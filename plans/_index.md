# Implementation plans

Implementation plans for WICA — each plan turns a settled part of a spec (see [specs/_index.md](../specs/_index.md)) into concrete, buildable steps. Plans are ordered by their date-time filename prefix (`YYYYMMDDHHmm_`).

## Plans

| Plan | Description | Status |
|---|---|---|
| [202607201819_world-registry-implementation.md](202607201819_world-registry-implementation.md) | World registry data model + API (`register`/`update`/`get`/listeners/triggers), `get_world()` singleton | Done |
| [202607231408_world-content-model.md](202607231408_world-content-model.md) | Neutral `Content` model (`TextPart`/`ImagePart`) and migrating World serialization/rendering from `str` to `Content`, with fresh/archival split | Done |
| [202607231754_e2e-test-framework.md](202607231754_e2e-test-framework.md) | Reusable `tests-e2e/` scaffolding: skip-without-credentials mechanics and a real-chat-model helper | Done |
| [202607231801_agent-v1-implementation.md](202607231801_agent-v1-implementation.md) | Agent v1 reasoning loop: LangChain-backed inference over World, snapshot history, event-driven async tools, output sink | Done |
| [202607232000_command-abstraction.md](202607232000_command-abstraction.md) | Command abstraction: new `commands.md` spec, reframe agent.md "Tools" → "Commands", rename code (`register_command`, `CommandExecution`, `agent:command:<id>`) — tools become the under-the-hood primitive | Done |
| [202607232308_gradio-conversation-demo.md](202607232308_gradio-conversation-demo.md) | First runnable example ([specs/conversation-demo.md](../specs/conversation-demo.md)): Gradio conversation UI over a simulated social robot, live World/prompt panels, sensor inputs + robot-action Commands; adds an `Agent.on_prompt` debug hook | Done |
| [202607281051_config-from-json.md](202607281051_config-from-json.md) | Framework config from JSON ([specs/config.md](../specs/config.md)): `config.py` with `WicaConfig`/`AgentConfig`, strict `from_json` loader, `system_prompt_file`, `api_key`/`api_key_env`; migrates the conversation demo and e2e tier to committed config files, each composing `WicaConfig.from_json` + `Agent.from_config` directly (no `from_config_file`) | Done |
| [202607291530_demo-prompt-history.md](202607291530_demo-prompt-history.md) | Conversation demo Prompt panel keeps every step's prompt ([specs/conversation-demo.md](../specs/conversation-demo.md)): a dropdown labelled by time + trigger, auto-snapping the view to the newest step | Done |
| [202607291700_command-outcome-via-observation.md](202607291700_command-outcome-via-observation.md) | Fix "doesn't know a command is still running" ([specs/commands.md](../specs/commands.md), [specs/agent.md](../specs/agent.md)): Command `tool_result` becomes a fixed ack, outcome/status delivered by the `agent:command:<id>` entry rendered as an observation (never skipped), via a `serialize_fn` override on `render_entry`; removes `_command_results` | Done |
| [202607301100_model-issued-cancel-command.md](202607301100_model-issued-cancel-command.md) | Model-issued `cancel_command(call_id)` ([specs/commands.md](../specs/commands.md), [specs/agent.md](../specs/agent.md)): auto-registered WICA-native Command exposing the Agent's existing loop-thread cancellation to the LLM; targets a running command by its already-visible `call_id`, lenient on the entry-key prefix, id-guarded no-op, no render change | Done |
| [202607301221_trigger-coalescing-window.md](202607301221_trigger-coalescing-window.md) | Trigger coalescing ([specs/agent.md](../specs/agent.md), resolves [specs/world.md](../specs/world.md) OQ#1): Agent batches a burst of triggers into one step via a fixed leading-edge `coalesce_window` (default 200 ms, `0` disables); per-entry `bypass_coalescing` flushes the window early; `on_trigger` fires once per collected trigger; still drop-when-busy | Done |
| [202608140914_multi-provider-openai-huggingface-hub.md](202608140914_multi-provider-openai-huggingface-hub.md) | Add `openai` (`langchain-openai`, uniform `init_chat_model`) and `huggingface-hub` (`langchain-huggingface`, dedicated `ChatHuggingFace`+`HuggingFaceEndpoint` branch, hosted-endpoint only) providers ([specs/config.md](../specs/config.md), [specs/project.md](../specs/project.md), [specs/agent.md](../specs/agent.md)): provider extras, `hf_provider` config field, no capability probe | Done |
| [202608201049_fake-provider.md](202608201049_fake-provider.md) | Deterministic `provider: "fake"` scripted `BaseChatModel` ([specs/fake-provider.md](../specs/fake-provider.md)): new public `src/wica/fake_model.py`, `build_chat_model` branch, `model_kwargs` script/default/loop/`delay_s` (200 ms), lazy `bind_tools` name check; fast `tests/test_fake_model.py` unit tests + always-run `tests-e2e/test_fake_flows.py` flow suite (`-k fake`, kept out of `PROVIDER_CONFIGS`); config.md + INTEGRATING.md + e2e-tier docs | Done |
| [202608211316_config-lazy-resolution.md](202608211316_config-lazy-resolution.md) | Make config dataclasses a 1:1 JSON mirror and move resolution from load to Agent build ([specs/config.md](../specs/config.md), [specs/agent.md](../specs/agent.md)): `AgentConfig` keeps `api_key`/`api_key_env`/`system_prompt`/`system_prompt_file` verbatim, `from_*` validates only, `from_json` locates (absolutizes) the prompt path without reading it; new `resolve_api_key`/`resolve_system_prompt` helpers called by `build_chat_model`/`Agent.from_config`, so `MissingEnvError`/unreadable-file surface at build; demo + e2e `try` sites move | Done |
| [202608221100_wica-facade.md](202608221100_wica-facade.md) | The `Wica` facade + lifecycle refactor ([specs/wica.md](../specs/wica.md) new; [world.md](../specs/world.md)/[agent.md](../specs/agent.md)/[events.md](../specs/events.md)/[config.md](../specs/config.md) updated): new `src/wica/wica.py` (`Wica.init`/`start`/`stop`/`register_command`; surfaces four `Event`s `on_world_trigger`/`on_agent_trigger`/`on_agent_prompt`/`on_agent_command`; `CommandIssued` in `agent.py`); Wica owns one shared event loop injected into both World and Agent; World gains `start`/`stop`/`is_running`, dispatches sync/async listeners on the loop (drops its executor), replaces `set_trigger_handler` with an `on_trigger` Event, and loses the `get_world`/`reset_world` singleton; `Agent.__init__` takes `AgentConfig` (drops `from_config`), runs on the injected loop (no owned thread/bridge), turns its hooks into Events and subscribes to `world.on_trigger`; `Event.emit` isolates subscribers; migrates demo + fake-flow e2e + conftests/e2e support; realigns four downstream specs (inputs/testing/conversation-demo/fake-provider) | Done |

## Status legend

- **Todo** — written, not yet started
- **In progress** — actively being implemented
- **Done** — implemented, verified (lint/type-check/tests pass), and merged
